{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "cd EcoAula-front && npm ci",
  "buildCommand": "cd EcoAula-front && npm run build",
  "outputDirectory": "EcoAula-front/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
